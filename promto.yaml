name: fincalendar
comment: Финансовый календарь — React SPA + Express payment backend

services:
  web:
    friendly_name: "FinCalendar — Веб-приложение"
    type: frontend
    framework: react
    project_dir: /home/user/FinCalendar
    build_command: npm install && npm run build
    publish_dir: dist

  api:
    friendly_name: "Payment API (YooKassa)"
    type: backend
    framework: express
    runtime: node
    deployment_strategy: native
    project_dir: /home/user/FinCalendar/server
    start_command: node index.js
    env_vars:
      - name: PORT
        secret: false
      - name: YOOKASSA_SHOP_ID
        secret: true
      - name: YOOKASSA_SECRET_KEY
        secret: true
      - name: APP_URL
        value: "https://appfincalendar.ru"
        secret: false
